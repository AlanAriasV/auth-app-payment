import { Router } from 'express';

const purchaseRouter = Router();

purchaseRouter.post('/', async (req, res) => {});

purchaseRouter.put('/', async (req, res) => {});

export default purchaseRouter;
